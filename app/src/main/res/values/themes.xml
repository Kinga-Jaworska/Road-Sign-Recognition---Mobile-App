<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ImageOwnT3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->

        <!-- Bottom Color -->
        <item name="colorPrimary">@color/white</item>

        <!-- Top Color -->
        <item name="colorPrimaryVariant">@color/LightGreen</item>

        <item name="colorOnPrimary">@color/whiteTransparent</item>

        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/LightGreen</item>

        <!--Text/Img Button -->
        <item name="colorSecondaryVariant">@color/white</item>

        <!-- Button Switches-->
        <item name="colorSecondary">@color/LightGreen</item>

        <!-- wifi -->
        <item name="colorAccent">@color/LightGreen</item>
        <item name="android:button">@color/LightGreen</item>
        <item name="android:textColorSecondary">@color/darkColor</item>
        <item name="android:textColor">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
    </style>
</resources>